
<% if current_user.has_role?(:zonal_admin, current_user.zone) %>
  <%= link_to "View Zone #{current_user.zone.name} admin page", zonal_admin_root_path, class: "btn btn-light mg-l-5" %>
<% end %>

<% if current_user.has_role?(:chapter_admin, current_user.chapter) %>
  <%= link_to "View #{current_user.chapter.name} admin page", chapter_admin_root_path, class: "btn btn-light mg-l-5" %>
<% end %>
